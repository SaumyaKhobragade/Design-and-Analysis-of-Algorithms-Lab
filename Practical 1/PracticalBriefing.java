public class PracticalBriefing {
    public static void main(String[] args) {
        // AIM: Time and complexity analysis of loops for a sensor data monitoring system by generating random sensor readings such as temperature and pressure.
        // The goal is to analyze and compare the performance pf different algorithms.

        // Addition of TTwo Numbers
        int a = 3, b = 2, c;
        c = a + b;
        System.out.println(c);

        // If the algorithm has a countable and finite number of code, then time complexity required is order of 1
        //To represent the time complexity, we need to draw a 2 dimensional graph in which x axis represent the line of code and y axis represent the time bound

        // Single For Loop
        int i, n = 10;
        for (i = 0; i < n; i++) {
            System.out.println("CSE");
        }
    }
}
